const mathUtils = require('./mathUtils');

const a = 10;
const b = 5;

console.log(`Soma: ${mathUtils.soma(a, b)}`);
console.log(`Subtração: ${mathUtils.subtracao(a, b)}`);
console.log(`Multiplicação: ${mathUtils.multiplicacao(a, b)}`);
console.log(`Divisão: ${mathUtils.divisao(a, b)}`);
console.log(`Exponenciação: ${mathUtils.exponenciacao(a, b)}`);
