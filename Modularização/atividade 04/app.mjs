import { soma, subtracao, multiplicacao, divisao, exponenciacao } from './mathUtils.mjs';

const a = 10;
const b = 5;

console.log(`Soma: ${soma(a, b)}`);
console.log(`Subtração: ${subtracao(a, b)}`);
console.log(`Multiplicação: ${multiplicacao(a, b)}`);
console.log(`Divisão: ${divisao(a, b)}`);
console.log(`Exponenciação: ${exponenciacao(a, b)}`);
